package trykotlin

import android.util.Log

/**
 *  We declare a package-level functiion main which returns Unit and tasks
 *  an Array of Strings as a parameter.Note that semicolons are optional.
 */
fun main() {
    Log.d("mangokt","Hello world!")
}